# TrigMem Categories - Les 6 Types d'Information

> **Invocable par** : `/trigmem-categories`
> **Type** : Classification Support
> **Déclenchement** : Automatique (détecte la catégorie)

---

## 🎯 CE QUE CE SKILL FAIT

Ce skill identifie automatiquement dans quelle catégorie appartient votre information, selon les 6 types définis par TrigMem.

Il est appelé automatiquement quand vous essayez de stocker quelque chose et que Claude a besoin de comprendre **QUOI** c'est.

---

## 📊 LES 6 CATÉGORIES

### Catégorie 1 : Identité Projet

**Questions clés :**
- "Qu'est-ce que c'est ce projet ?"
- "Quel est le tech stack ?"
- "Quel est le but de ce projet ?"

**Exemples :**
```markdown
- Nom: ShopFlow
- Stack: Next.js 14, Prisma, PostgreSQL, Stripe
```

```markdown
- Nom: PetTracker API
- Stack: Go, gRPC, MongoDB, GCP
```

---

### Catégorie 2 : Structure Codebase

**Questions clés :**
- "Où sont les composants ?"
- "Quelle est la structure des dossiers ?"

**Exemples :**
```markdown
# Monorepo
packages/
├── web/          # Next.js frontend
├── api/          # Express backend
├── shared/       # Shared types and utilities
└── mobile/       # React Native app
```

---

### Catégorie 3 : Commandes Opérationnelles

**Questions clés :**
- "Comment lancer les tests ?"
- "Comment déployer ?"
- "Comment builder ?"

**Exemples :**
```bash
# Lancer tests
pnpm test

# Déployer
./scripts/deploy-staging.sh
```

---

### Catégorie 4 : Patterns Réutilisables

**Questions clés :**
- "Quelle est l'architecture ?"
- "Quel pattern utilises-vous ?"

**Exemples :**
```markdown
## Architecture Hexagonale
Domain layer contains pure business rules. Ports define interfaces. Adapters implement ports.
Dependencies point inward (adapters depend on ports, not vice versa).
```

```markdown
## TDD Workflow
1. Write a failing test for new behavior
2. Write minimal code to make test pass
3. Refactor while keeping tests green
4. Repeat
```

---

### Catégorie 5 : Guides Architecturales

**Questions clés :**
- "Comment est-ce implémenté ici ?"
- "Quelles conventions utilise-t-on ?"

**Exemples :**
```markdown
# Conventions de composants
ComponentName/
├── index.tsx         # Main component
├── ComponentName.test.tsx
├── ComponentName.styles.ts
└── types.ts
```

---

### Catégorie 6 : Corrections Itératives

**Questions clés :**
- "Est-ce que je continue à faire cette erreur ?"
- "Est-ce que Claude utilise ce mauvais pattern ?"

**Exemples :**
```markdown
## Langage/Framework Quirks
Don't use 'any' - use 'unknown' and narrow type.
```

---

## 🎯 COMMENT UTILISER CE SKILL

### Invocation Automatique

Ce skill s'active automatiquement quand Claude détecte que vous essayez de stocker quelque chose. Vous n'avez pas besoin de l'invoquer manuellement - il pose automatiquement les bonnes questions.

**Exemples :**

```
Vous: "J'ai créé un nouveau composant"
Skill détecte: Catégorie 4 (Patterns Réutilisables)
Pose: "Quelle est l'architecture ? (Hexagonale, Layered, etc.)"
```

---

## 🔧 DÉCLANCHEMENT POSSIBLE

Si vous souhaitez un contrôle plus manuel sur l'invocation, utilisez plutôt :

```markdown
/trigmem-storage  - Pour choisir manuellement le mécanisme
```

---

## 📚 EN SAVOIR PLUS

Pour une maîtrise complète, continuez avec :
- `/trigmem-decision` - Guide de décision
- `/trigmem-storage` - Options de stockage
- Et les autres skills TrigMem...
