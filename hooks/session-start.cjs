#!/usr/bin/env node
/**
 * SessionStart Hook - SMITE Welcome Message
 */

console.error(`
✨ SMITE v1.6.6 - Enhanced Configuration
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 Active Features:
  ✓ Agent Teams (parallel execution)
  ✓ TrigMem Skills (auto-activation)
  ✓ Git Flow Master (versioned commits)
  ✓ MCP Integration (claude-mem, chrome-devtools)

💡 Quick Commands:
  /studio build "task"        → Auto-detected implementation
  /studio build --scale       → Thorough EPCT workflow
  /studio build --team        → Parallel agents
  /pattern nextjs             → Load Next.js patterns

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`);

process.exit(0);
